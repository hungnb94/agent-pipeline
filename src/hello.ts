/**
 * Print the canonical greeting. Used by the `agent-pipeline hello` subcommand
 * and as a smoke-test target for the pipeline itself.
 */
export function printHello(): string {
  return 'Hello World\n';
}
